'use strict'; 

define(['app'], function (app) {
    app.controller('SolucoesCtrl', function ($scope, $rootScope) {
        
        $scope.textPage = 'Soluções';
        
                       
    });
});