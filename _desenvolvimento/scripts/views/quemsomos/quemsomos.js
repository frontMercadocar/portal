'use strict'; 

define(['app'], function (app) {
    app.controller('QuemSomosCtrl', function ($scope, $rootScope) {
        
        $scope.textPage = 'Quem Somos';
        
        
        
    });
});