'use strict'; 

define(['app'], function (app) {
    app.controller('DCCCtrl', function ($scope, $rootScope) {
        
        $scope.textPage = 'Página D.C.C';
        //console.log($scope.textPage);
                       
    });
});

